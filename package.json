{
  "name": "@standardnotes/secure-utils",
  "private": true,
  "engines": {
    "node": ">=14.0.0 <17.0.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start --parallel",
    "test": "lerna run test --parallel",
    "coverage": "lerna run coverage --parallel",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "postinstall": "lerna run postbuild"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
